[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npx pnpm@10.4.1 install --frozen-lockfile"]

[phases.build]
cmds = ["npx pnpm@10.4.1 run build"]

[start]
cmd = "npx tsx scripts/seed-users.ts && NODE_ENV=production node dist/index.js"
